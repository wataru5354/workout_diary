<%= render 'shared/header.html.erb' %>

<div class="p-4 p-md-5 pb-4 text-white rounded bg-dark">
    <div class="col-md-6 px-0">
      <p class="display-4 fst-italic fs-2">Fitness is not about being better than someone else. It’s about being better than you used to be.</p>
      <p class="fs-4">ーフィットネスは、他人より優れることが重要ではない。かつての自分を超越するものだ。</p>
    </div>
</div>
<div class="bg-light">
  <div class="fs-2 text-center pt-4">ワークアウト記録一覧
  </div>
  <div class="container">
    <div class="mx-auto">
      <% @diaries.each do |diary|%>
      <div class="col-12">
        <%= link_to diary_path(diary), class:"card mt-5 mx-auto text-decoration-none link-dark", style:"width: 30em;"  do %>
          <% if diary.image.present?%>
            <% if diary.image.content_type.include?('image/')%>
            <%= image_tag diary.image,class:"card-img-top image-fluid" %>
            <% else %>
            <%= video_tag rails_blob_path(diary.image), class: "card-img-top image-fluid",controls: true %>
            <% end%>
          <% end %>
          <div class="text-center fw-bold">
            <%= diary.start_time.strftime("%Y年%m月%d日")%>
            <div class="mt-2">
              部位: <%= diary.site%>
            </div>
            <div class="mt-2">
            <%= diary.user.nickname %>
            </div>
          </div>
        <% end %>
      </div>
      <% end %>
    </div>
  </div>
</div>

<div class= "d-flex justify-content-end fixed-bottom" >
<%= link_to(new_diary_path,  class: "btn btn-primary rounded-circle p-0 me-5 mb-4 pt-3", :style => "width:7rem;height:7rem;") do%>
  <span class="ms-2">記録する</span>
  <%= image_tag "diary.svg", class: "mt-2" , :size  => "40x40" %>
<% end%>
</div>